ktor {
  application {
    modules = [ com.letapay.backend.ApplicationKt.module ]
  }
  deployment {
    port = ${?PORT}
    port = 8080
    host = "0.0.0.0"
  }
}

koog {
  openai {
    apiKey = ${?OPENAI_API_KEY}
    baseUrl = "https://api.openai.com"
  }
}

ai {
  models {
    parse = ${?AI_PARSE_MODEL}
    parse = "gpt-4o-mini"
    plan = ${?AI_PLAN_MODEL}
    plan = "gpt-4o"
    chat = ${?AI_CHAT_MODEL}
    chat = "gpt-4o-mini"
  }
}

database {
  url = ${?DATABASE_URL}
  driver = ${?DATABASE_DRIVER}
  user = ${?DATABASE_USER}
  password = ${?DATABASE_PASSWORD}
  minPoolSize = ${?DB_MIN_POOL_SIZE}
  maxPoolSize = ${?DB_MAX_POOL_SIZE}
  connectionTimeoutMs = ${?DB_CONNECTION_TIMEOUT_MS}
}

redis {
  url = ${?REDIS_URL}
}

security {
  jwt {
    issuer = ${?JWT_ISSUER}
    audience = ${?JWT_AUDIENCE}
    secret = ${?SESSION_SECRET}
  }
  refresh {
    pepper = ${?REFRESH_TOKEN_PEPPER}
  }
}

agentkit {
  sidecarUrl = ${?AGENTKIT_SIDECAR_URL}
  sidecarUrl = "http://agentkit-sidecar:3100"
  sidecarSecret = ${?SIDECAR_SECRET}
}

transactions {
  maxValueEth = ${?MAX_TRANSACTION_VALUE_ETH}
}
